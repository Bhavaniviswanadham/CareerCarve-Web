// Example service for booking-related operations
export const bookSlot = (student, mentor, time) => {
  // Implement booking logic
  console.log(`Booked ${mentor.name} at ${time} for ${student.name}`)
}
